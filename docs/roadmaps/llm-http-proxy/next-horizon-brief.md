# Next-horizon brief (for horizon 8) — prepared Stage 3.5, horizon 7

## Unknowns
- Do the four new *.integration.test.ts files actually pass against real provider APIs in a developer shell, or does any single one fail (rate-limit, model-name drift, content-type quirks, network firewall) — and if so, does the failure reveal a latent package bug or a config-only issue?
- Does the post-horizon-7 `npm test` (default gate, no env vars) still complete in the same wall-clock time as the recorded 126-test baseline, or does describe.skip overhead measurably creep up across four new files now that discovery's testRegex DOES match `*.integration.test.ts` (the pre-horizon assumption of natural non-discovery was false)?
- After the integration tests exist, is the horizon-5 p99 FAIL (173.88% of baseline, attributed to environmental noise) still the open latency item, or does the smoke-test end-to-end exercise change anything that lets the bar be declared met?
- Does asserting `JSON.stringify(entries[0])` does NOT contain the API key value actually prove safety, or does the Gemini URL-key path still leak the key somewhere else (e.g. a raw capture fallback, a debug emitter, a future maskedResponseBody change)?
- Is the package still publish-clean after this horizon (no new public API, no version bump, dist still absent), or does adding integration-test discipline (e.g. key-leak defense assertions, live-only assertions) need a corresponding docs bump in the README that was deferred by the user redirect?
- Did adding the live tests push the package off 0.2.0 semver-wise — the tests reference LlmLogEntry fields and provider URLs that could quietly become a stability contract — or is the surface still open to evolve as documented?

## Research (before planning horizon 8)
- Re-run `npm test` from packages/llm-http-proxy after this horizon lands to capture the new baseline: `npx jest --listTests | wc -l` (should be 11, up from 7) and the wall-clock of the default run (should match horizon-6's recorded time to within ms) so any drift is measurable and the success bar's regression clause is verifiable.
- Re-read each new packages/llm-http-proxy/src/*.integration.test.ts to confirm: (a) the gate shape is the verbatim RUN_BENCH describe.skip pattern (not a jest config tweak); (b) each test awaits two setImmediate ticks before asserting; (c) the timeout is 30-60s; (d) `capturePayloads` default-off is honored unless the test explicitly opts in; (e) the Gemini test asserts `JSON.stringify(entries[0])` excludes the key value read from process.env.
- Re-read packages/llm-http-proxy/src/interceptor.ts deriveUrl path-handling (around line 961, `const path = view.path ?? '/'`) to confirm whether the URL field of LlmLogEntry for a Gemini call does or does not carry the `?key=...` query string — this is the load-bearing safety claim that the Gemini test is built on, and a regression here would invalidate the test's intent.
- Re-read docs/roadmaps/llm-http-proxy/blockers.md after horizon-7 to enumerate which open items still gate a real `npm publish` (semver-freeze, ESM dist lazy-load inertness, prepack/prepublishOnly auto-build, repository/homepage/bugs/publishConfig fields, consumer-facing OTEL README docs, optional-peer install resolution) versus which are now post-publish product decisions.
- Re-read packages/llm-http-proxy/package.json to confirm: `testRegex` is unchanged (the discovery said the gate relies on describe.skip, not on testPathIgnorePatterns); `lint` glob still picks up the new files (so they must lint clean); `test` script is still bare `jest --passWithNoTests`; no new package-lock.json was committed (zero-lockfile convention preserved).
- Read packages/llm-http-proxy/dist/esm/otel.js (and dist/cjs/otel.js) after a fresh `npm run build` to confirm the OTEL span exporter still lazy-loads via try/catch require under both emit targets — horizon-6's untested ESM runtime path is still open and a horizon-7-side build is the cheapest way to re-check it.
- Re-run `npm view llm-http-proxy` against the public registry at planning time to confirm name availability hasn't changed since horizon-5 (2026-08-29, HTTP 404) — availability is not static and the publish bar cannot start while this is unverified.
- Re-read docs/roadmaps/llm-http-proxy/benchmark-results.md to confirm the horizon-5 p99 FAIL is still the recorded state and decide whether the next horizon owes a clean re-run or can declare the bar environmentally-noise-closed.

## Decisions needed (horizon 8 cannot avoid these)
- Whether the next horizon should now target the last unexecuted full-objective bar — a real `npm publish` — or whether the live smoke tests (this horizon) themselves are a publishable user-facing capability that warrants a 0.2.0 → 0.3.0 version bump first to honor semver.
- Whether the integration tests are documented as a public consumer affordance (added to README's testing section, mentioned in index.ts doc comments) or kept as an internal-only developer tool, and what that choice does to the public API stability story.
- Whether to keep the integration-test opt-in gate as the RUN_BENCH-style describe.skip pattern (current state per this horizon) or finally add the deferred `testPathIgnorePatterns: ['integration\\.test\\.ts$']` jest config tweak — the discovery rules out the latter for this horizon but the next horizon is the natural place to revisit if discover-but-skip overhead becomes measurable or if a sixth provider test is added.
- Whether the OTEL full-objective bar is closed by horizon-6's InMemorySpanExporter proof (no wire OTLP/HTTP/gRPC needed) or whether a live collector round-trip is now owed given the live-test infrastructure this horizon just put in place (the same opt-in pattern could plausibly host a wire test).
- Whether to resolve the horizon-5 p99 FAIL this next horizon via a clean re-run (or by amending the methodology to exclude interleaved install/restore churn), or accept it as a recorded environmental miss and close the latency bar by decision rather than re-measurement.
- Whether the still-open blockers (semver-freeze, ESM dist inertness, prepack auto-build, repository metadata fields, consumer-facing OTEL docs) all gate a real publish together, or whether some can be deferred to a post-publish product decision — and in what order to resolve the ones that do gate.
- Whether the next horizon should plan a real `npm publish` (irreversible, registry-visible) or another `npm publish --dry-run` plus tarball inspection (cheap, reversible) given that the package is still 0.2.0 and unpublished.

## Artifacts to inspect
- /Users/dimitrykatz/workspace/elenwatch/packages/llm-http-proxy/src/anthropic.integration.test.ts
- /Users/dimitrykatz/workspace/elenwatch/packages/llm-http-proxy/src/openai.integration.test.ts
- /Users/dimitrykatz/workspace/elenwatch/packages/llm-http-proxy/src/gemini.integration.test.ts
- /Users/dimitrykatz/workspace/elenwatch/packages/llm-http-proxy/src/fetch-baseline.integration.test.ts
- /Users/dimitrykatz/workspace/elenwatch/packages/llm-http-proxy/src/benchmark.test.ts (RUN_BENCH gate reference, lines 317-366)
- /Users/dimitrykatz/workspace/elenwatch/packages/llm-http-proxy/src/interceptor.test.ts (two-setImmediate flush pattern reference)
- /Users/dimitrykatz/workspace/elenwatch/packages/llm-http-proxy/src/options.ts (LlmLogEntry shape, lines 18-31)
- /Users/dimitrykatz/workspace/elenwatch/packages/llm-http-proxy/src/interceptor.ts (deriveUrl path handling around line 961, kNoCapture, emission guards)
- /Users/dimitrykatz/workspace/elenwatch/packages/llm-http-proxy/package.json (jest testRegex, scripts, peerDependencies, optional-peer metadata)
- /Users/dimitrykatz/workspace/elenwatch/packages/llm-http-proxy/README.md (consumer-facing surface, OTEL docs absence)
- /Users/dimitrykatz/workspace/elenwatch/packages/llm-http-proxy/dist/esm/otel.js (untested ESM runtime path)
- /Users/dimitrykatz/workspace/elenwatch/docs/roadmaps/llm-http-proxy/blockers.md (open publish gates)
- /Users/dimitrykatz/workspace/elenwatch/docs/roadmaps/llm-http-proxy/benchmark-results.md (horizon-5 p99 FAIL)
- /Users/dimitrykatz/workspace/elenwatch/docs/roadmaps/llm-http-proxy/discoveries.md (horizon-6 ESLint meta-warning, OTEL API 2.10.0 constructor-only processors, package-lock gitignore convention)

## Recommended next-horizon scope
The next horizon should target the one remaining unexecuted full-objective bar — a real `npm publish` (or a final `npm publish --dry-run` plus tarball inspection if the irreversibility is judged too high) — since publish-hygiene debt is closed (horizon-6 README + author), the OTEL demo is closed via InMemorySpanExporter (horizon-6), and this horizon now adds documented end-to-end provider coverage that a real consumer would want to see in the package README. It should spend its early phases capturing the post-horizon-7 baseline (test count, wall-clock, lint/typecheck/build all green with the 4 new files discovered) and confirming the live-test gate is genuinely ms-fast under default `npm test`, then close the publish bar by resolving whichever blockers actually gate a real publish — semver-freeze call, ESM dist lazy-load inertness proof via a genuine ESM smoke test, prepack/prepublishOnly auto-build if the package must publish from a fresh clone, and the minimum repository/homepage/publishConfig metadata fields — before running the publish or dry-run. It should hold off on OTLP/HTTP wire export, trace propagation/context injection, additional providers (Mistral/Cohere), shared live-test-harness extraction, the jest testPathIgnorePatterns config tweak, streaming/auth-error/rate-limit deepening per provider, and consumer-facing OTEL README docs until the publish bar lands and the next-horizon decisions above are resolved. Roughly 3-5 phases: one verification phase to capture the post-horizon-7 baseline, one or two phases to resolve the publish-gating blockers, and one final phase to execute the publish (real or dry-run) and commit the result — no new feature code, no per-provider deepening.
