# Grounded discoveries
- 2026-08-28 | horizon 1 | module inventory | src/llm-http-logging/ is exactly 2 files (~265 LOC); existing capabilities (model, tokens, url, caller trace) map 1:1 to the provider-parser registry [src/llm-http-logging/llm-http-interceptor.service.ts] -> port is a small rewrite, not a large refactor
- 2026-08-28 | horizon 1 | public API contract | options/entry shape is LlmHttpLoggingOptions + LlmLogEntry{timestamp,model,inputTokens,outputTokens,callerTrace,url} via LlmHttpLoggingModule.register [src/llm-http-logging/llm-http-logging.module.ts] -> carry into the package retrofitted with parser/logger/redaction seams
- 2026-08-28 | horizon 1 | NestJS coupling | hard @nestjs/common deps (@Injectable, OnModuleInit, DynamicModule); only consumer is app.module.ts register() [src/llm-http-logging/llm-http-logging.module.ts] -> sever decorators+factory; optional thin Nest wrapper deferred to horizon 2
- 2026-08-28 | horizon 1 | packaging realities | no packages/ layout, no lockfile, root private:true; root tsconfig/jest are Nest-centric and not reusable [package.json] -> package scaffold gets its own dual ESM/CJS tsconfig, jest, eslint, lockfile
- 2026-08-28 | horizon 1 | existing gaps | zero @opentelemetry/*, AsyncLocalStorage, SSE, redaction, or transformer code; error handler is empty, no spec, no benchmark, no README [src/llm-http-logging/llm-http-interceptor.service.ts] -> all extension/security/benchmark work is greenfield; error-path logging must not regress today's silent data loss
- 2026-08-28 | horizon 1 | latency detail | today's design already does NO blocking work on the request path (capture in write/end/data callbacks after originalRequest); req.protocol url bug exists; no Content-Length accounting [src/llm-http-logging/llm-http-interceptor.service.ts] -> preserve off-request-path ordering; fix url derivation; transformer work must handle Content-Length
- 2026-08-28 | horizon 1 | interceptor-core | captured proto.prototype.{write,end,on}.bind(proto.prototype) crashes Node's stream internals — bound function locks `this` to the prototype and ignores `.call(thisArg,...)`, so the real implementation's first `this._header` access dies [packages/llm-http-proxy/src/interceptor.ts] -> MUST capture unbound prototype methods and forward via `.call(this, ...)` so the actual ClientRequest becomes the receiver; this is the canonical "unbound monkey-patch" pitfall
- 2026-08-28 | horizon 2 | interceptor | emitLogEntry calls deriveUrl(req) with no scheme so all HTTPS provider calls log http://; deriveUrl(req, scheme='http') is already scheme-aware [packages/llm-http-proxy/src/interceptor.ts] -> fix at the call site only (horizon 2 phase 1)
- 2026-08-28 | horizon 2 | redaction | DEFAULT_SENSITIVE_FIELDS contains bare 'token' and isSensitiveKey substring-matches, so *_tokens accounting fields get redacted; access_token/refresh_token are also listed explicitly [packages/llm-http-proxy/src/redaction.ts] -> drop bare 'token' (horizon 2 phase 0)
- 2026-08-28 | horizon 2 | redaction | walk() adds every visited node to the seen WeakSet and never deletes it, so a DAG second-reference becomes the cycle placeholder [packages/llm-http-proxy/src/redaction.ts] -> add-before-recurse / delete-after in both array and object branches (horizon 2 phase 0)
- 2026-08-28 | horizon 2 | emission path | emitLogEntry only try/catches the logger() call; deriveUrl/resolveParser/parseCall/redact run unguarded inside the setImmediate [packages/llm-http-proxy/src/interceptor.ts] -> wrap the whole body (horizon 2 phase 3)
- 2026-08-28 | horizon 2 | request reflection | reflectCall drops the encoding argument when both an encoding and a callback are supplied [packages/llm-http-proxy/src/interceptor.ts] -> forward via original.apply(receiver, args) (horizon 2 phase 2)
- 2026-08-28 | horizon 2 | emission path | completeCapture has no once-guard; response 'end' and req 'error' both call it -> two entries, the second carrying entry.error [packages/llm-http-proxy/src/interceptor.ts] -> state.emitted flag (horizon 2 phase 3)
- 2026-08-28 | horizon 2 | chunk buffering | appendChunk decodes each Buffer chunk independently so a multi-byte char split across a chunk boundary corrupts to U+FFFD [packages/llm-http-proxy/src/interceptor.ts] -> buffer Buffer[] and decode once (horizon 2 phase 4)
- 2026-08-28 | horizon 2 | capture decision | the patch is on ClientRequest.prototype.write so shouldCapture re-runs on every write of every non-matching request; onWrapper is an easily-overlooked third call site [packages/llm-http-proxy/src/interceptor.ts] -> kNoCapture negative cache (deferred #8)
- 2026-08-28 | horizon 2 | provider parser | openai/anthropic/cohere/mistral/fallbackParser are all === genericChatParser; PARSERS_BY_HOST + resolveParser always return the same object; parserToTokenCounter is exported but dead [packages/llm-http-proxy/src/provider-parser.ts] -> collapse to one defaultParser (deferred #9)
- 2026-08-28 | horizon 2 | app-module wiring | src/app.module.ts computes sampleParser/sampleParseResult/maskedSample at import then discards via void, and runs llmHttpInterceptor.install() at import time [src/app.module.ts] -> move to a bootstrap hook (deferred #10)
- 2026-08-28 | horizon 2 | build/test setup | packages/llm-http-proxy is not a workspace member (own package.json/eslint/3 tsconfigs/lockfile, Jest ts-jest inlined); root consumes via link: so 'green at both levels' needs a package dist rebuild before root nest build [packages/llm-http-proxy/package.json]
- 2026-08-28 | horizon 2 | test helpers | tests use real http.createServer on 127.0.0.1 + a setImmediate flush; no fake ClientRequest helper exists -- public attachCapture + a fake EventEmitter (on + getHeader) is the deterministic path for async-emission tests [packages/llm-http-proxy/src/interceptor.test.ts]
- 2026-08-28 | horizon 2 | git-state | commit "1" added "types":["node"] to packages/llm-http-proxy/tsconfig.json, hiding jest globals from tsc/eslint so all 4 .test.ts files failed compile; restored via "types":["node","jest"] [packages/llm-http-proxy/tsconfig.json] → baseline (79 tests, lint, typecheck, build) is green again, matching the roadmap's "stay green" regression bar
