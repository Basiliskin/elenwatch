# Grounded discoveries
- 2026-08-28 | horizon 1 | module inventory | src/llm-http-logging/ is exactly 2 files (~265 LOC); existing capabilities (model, tokens, url, caller trace) map 1:1 to the provider-parser registry [src/llm-http-lo — see horizon-1 roadmap.md
- 2026-08-28 | horizon 1 | public API contract | options/entry shape is LlmHttpLoggingOptions + LlmLogEntry{timestamp,model,inputTokens,outputTokens,callerTrace,url} via LlmHttpLoggingModule.register [src/llm-htt — see horizon-1 roadmap.md
- 2026-08-28 | horizon 1 | NestJS coupling | hard @nestjs/common deps (@Injectable, OnModuleInit, DynamicModule); only consumer is app.module.ts register() [src/llm-http-logging/llm-http-logging.module.ts] -> sev — see horizon-1 roadmap.md
- 2026-08-28 | horizon 1 | packaging realities | no packages/ layout, no lockfile, root private:true; root tsconfig/jest are Nest-centric and not reusable [package.json] -> package scaffold gets its own dual ESM/ — see horizon-1 roadmap.md
- 2026-08-28 | horizon 1 | existing gaps | zero @opentelemetry/*, AsyncLocalStorage, SSE, redaction, or transformer code; error handler is empty, no spec, no benchmark, no README [src/llm-http-logging/llm-http-in — see horizon-1 roadmap.md
- 2026-08-28 | horizon 1 | latency detail | today's design already does NO blocking work on the request path (capture in write/end/data callbacks after originalRequest); req.protocol url bug exists; no Content-Le — see horizon-1 roadmap.md
- 2026-08-28 | horizon 1 | interceptor-core | captured proto.prototype.{write,end,on}.bind(proto.prototype) crashes Node's stream internals — bound function locks `this` to the prototype and ignores `.call(thisAr — see horizon-1 roadmap.md
- 2026-08-28 | horizon 2 | interceptor | emitLogEntry calls deriveUrl(req) with no scheme so all HTTPS provider calls log http://; deriveUrl(req, scheme='http') is already scheme-aware [packages/llm-http-proxy/sr — see horizon-2 roadmap.md
- 2026-08-28 | horizon 2 | redaction | DEFAULT_SENSITIVE_FIELDS contains bare 'token' and isSensitiveKey substring-matches, so *_tokens accounting fields get redacted; access_token/refresh_token are also listed e — see horizon-2 roadmap.md
- 2026-08-28 | horizon 2 | redaction | walk() adds every visited node to the seen WeakSet and never deletes it, so a DAG second-reference becomes the cycle placeholder [packages/llm-http-proxy/src/redaction.ts] - — see horizon-2 roadmap.md
- 2026-08-28 | horizon 2 | emission path | emitLogEntry only try/catches the logger() call; deriveUrl/resolveParser/parseCall/redact run unguarded inside the setImmediate [packages/llm-http-proxy/src/interceptor. — see horizon-2 roadmap.md
- 2026-08-28 | horizon 2 | request reflection | reflectCall drops the encoding argument when both an encoding and a callback are supplied [packages/llm-http-proxy/src/interceptor.ts] -> forward via original.apply — see horizon-2 roadmap.md
- 2026-08-28 | horizon 2 | emission path | completeCapture has no once-guard; response 'end' and req 'error' both call it -> two entries, the second carrying entry.error [packages/llm-http-proxy/src/interceptor.t — see horizon-2 roadmap.md
- 2026-08-28 | horizon 2 | chunk buffering | appendChunk decodes each Buffer chunk independently so a multi-byte char split across a chunk boundary corrupts to U+FFFD [packages/llm-http-proxy/src/interceptor.ts] — see horizon-2 roadmap.md
- 2026-08-28 | horizon 2 | capture decision | the patch is on ClientRequest.prototype.write so shouldCapture re-runs on every write of every non-matching request; onWrapper is an easily-overlooked third call site — see horizon-2 roadmap.md
- 2026-08-28 | horizon 2 | provider parser | openai/anthropic/cohere/mistral/fallbackParser are all === genericChatParser; PARSERS_BY_HOST + resolveParser always return the same object; parserToTokenCounter is ex — see horizon-2 roadmap.md
- 2026-08-28 | horizon 2 | app-module wiring | src/app.module.ts computes sampleParser/sampleParseResult/maskedSample at import then discards via void, and runs llmHttpInterceptor.install() at import time [src/ap — see horizon-2 roadmap.md
- 2026-08-28 | horizon 2 | build/test setup | packages/llm-http-proxy is not a workspace member (own package.json/eslint/3 tsconfigs/lockfile, Jest ts-jest inlined); root consumes via link: so 'green at both leve — see horizon-2 roadmap.md
- 2026-08-28 | horizon 2 | test helpers | tests use real http.createServer on 127.0.0.1 + a setImmediate flush; no fake ClientRequest helper exists -- public attachCapture + a fake EventEmitter (on + getHeader) i — see horizon-2 roadmap.md
- 2026-08-28 | horizon 2 | git-state | commit "1" added "types":["node"] to packages/llm-http-proxy/tsconfig.json, hiding jest globals from tsc/eslint so all 4 .test.ts files failed compile; restored via "types": — see horizon-2 roadmap.md
- 2026-08-28 | horizon 3 | plan | provider-parser.ts has NO defaultParser export today; the generic parser is the unexported genericChatParser, and the 5 exported aliases are all === it [packages/llm-http-proxy/s — see horizon-3 roadmap.md
- 2026-08-28 | horizon 3 | plan | index.ts VERSION const = 0.1.0 and package.json version = 0.1.0, in sync; app.module.ts consumes it via the LLM_HTTP_PROXY_VERSION provider [packages/llm-http-proxy/src/index.ts] → #9 bump must update both
- 2026-08-29 | horizon 5 | npm availability | registry.npmjs.org/llm-http-proxy returned HTTP 404 on 2026-08-29 — the name is free to publish (checked at planning time; the decision phase re-checks at decision-wr — see horizon-5 roadmap.md
- 2026-08-29 | horizon 5 | bench opt-in mechanism | package jest config matches *.test.ts (ts-jest), *.bench.ts matches neither config, and ts-node exists only at root — so the harness ships as src/benchmark.test — see horizon-5 roadmap.md
- 2026-08-29 | horizon 5 | bench | @types/jest 29 has no describe.skipIf; the opt-in gate ships as `if (process.env.RUN_BENCH === '1') describe(...) else describe.skip(...)` — default npm test reports 1 suite/2 t — see horizon-5 roadmap.md
- 2026-08-29 | horizon 5 | bench result | RUN_BENCH=1 full run (1000 warmup/10000 measured, real patched path): request added p50=0.053ms (<1ms PASS), added p99=0.179ms = 177.6% of own baseline p99 (>2% FAIL); re — see horizon-5 roadmap.md
- 2026-08-29 | horizon 6 | packaging | package.json files[] references README.md but no such file exists on disk (only the root app README exists) and author is "" [packages/llm-http-proxy/package.json] -> publis — see horizon-6 roadmap.md
- 2026-08-29 | horizon 6 | packaging | package has zero peerDependencies, dependencies {}, and zero @opentelemetry references; the repo has zero lockfiles anywhere [packages/llm-http-proxy/package.json] -> the op — see horizon-6 roadmap.md
- 2026-08-29 | horizon 6 | packaging | dist/ is gitignored and absent and no prepack/prepublishOnly script exists [packages/llm-http-proxy/package.json, .gitignore] -> a dry-run must run npm run build first or it — see horizon-6 roadmap.md
- 2026-08-29 | horizon 6 | logger seam | logger.test.ts enforces no-@nestjs/no-raw source-grep invariants and the seam is exactly (entry: LlmLogEntry) => void with zero runtime imports [src/logger.ts, src/logger. — see horizon-6 roadmap.md
- 2026-08-29 | horizon 6 | packaging | npm 11.3.0 `npm publish --dry-run` exits 0 even when files[] lists a missing README.md (the file is silently omitted from the tarball, never an error), so the missing-README — see horizon-6 roadmap.md
- 2026-08-30 | horizon 6 | packaging | npm 11.3.0 `npm publish --dry-run` never prints the manifest (only tarball details), so optional-peer metadata must be proven from the packed tarball: `npm pack --pack-desti — see horizon-6 roadmap.md
- 2026-08-30 | horizon 6 | otel-span | the try/catch lazy `require('@opentelemetry/api')` is flagged by `@typescript-eslint/no-require-imports` (the current `recommendedTypeChecked` rule), NOT `@typescript-eslint — see horizon-6 roadmap.md
- 2026-08-30 | horizon 6 | otel-test | `@opentelemetry/sdk-trace-base@2.10.0` exposes NO public `addSpanProcessor()` method on `BasicTracerProvider` — the API moved to constructor-only `new BasicTracerProvider({ — see horizon-6 roadmap.md
- 2026-08-30 | horizon 7 | jest-discovery | testRegex in packages/llm-http-proxy matches *.integration.test.ts (the pre-horizon assumption of natural non-discovery was false); the RUN_BENCH describe.skip gate is — see horizon-7 roadmap.md
- 2026-08-30 | horizon 7 | RUN_BENCH-harness | gate = `const X = process.env.X === '1';` then `if (X) { describe(...) } else { describe.skip(...) }` with explicit test timeouts [src/benchmark.test.ts:317, 359-366 — see horizon-7 roadmap.md
- 2026-08-30 | horizon 7 | interceptor-install-and-capture | tests build local `Interceptor`, install/restore, await two setImmediate ticks; `withEntries` helper at src/interceptor.test.ts:75-91 [src/interceptor. — see horizon-7 roadmap.md
- 2026-08-30 | horizon 7 | LlmLogEntry-shape | fields: timestamp, model, inputTokens, outputTokens, callerTrace, url always; maskedRequestBody/ResponseBody only when capturePayloads:true [src/options.ts:18-31] → — see horizon-7 roadmap.md
- 2026-08-30 | horizon 7 | provider-request-shape-minimums | OpenAI POST /v1/chat/completions with `Authorization: Bearer`; Anthropic POST /v1/messages with `x-api-key` + `anthropic-version`; Gemini POST generate — see horizon-7 roadmap.md
- 2026-08-30 | horizon 7 | default-test-inventory | 7 *.test.ts files baseline; horizon-6 ledger: 126 package tests + 5 root tests in same wall-clock time [packages/llm-http-proxy/src/, state.md horizon-6 record] — see horizon-7 roadmap.md
- 2026-08-30 | horizon 7 | fetch-availability | `engines.node >=18`; global fetch routes through http.ClientRequest.prototype under Node 18+ [packages/llm-http-proxy/package.json] → fetch baseline uses global fetch directly with no shim
- 2026-08-30 | horizon 7 | package-scripts | lint glob `"src/**/*.ts"` includes new files; tsc includes src/ in tsconfig.json [packages/llm-http-proxy/package.json] → new files must lint clean and typecheck (env- — see horizon-7 roadmap.md
- 2026-08-30 | horizon 7 | no-new-exported-symbols | tests import from internal src/ paths only (`./interceptor`, `./options`); never via index.ts or resolved entry [src/logger.test.ts] → new integration tests mu — see horizon-7 roadmap.md
- 2026-08-30 | horizon 7 | gotchas | (1) capturePayloads:true needed for body assertions; (2) emission deferred via setImmediate, await TWO ticks; (3) callerTrace='unknown' under Jest; (4) inputTokens may be 0 fo — see horizon-7 roadmap.md
- 2026-08-30 | horizon 7 | npm_lifecycle_event-npx-side-effect | `npx jest ...` itself sets `process.env.npm_lifecycle_event='npx'`, overriding any prefix-set value; verified by adding a debug print to fetch-base — see horizon-7 roadmap.md
- 2026-08-30 | horizon 7 | fetch-baseline-contract-shift | The fetch baseline was originally planned as opt-in (gated on npm_lifecycle_event) firing one real fetch to https://example.com. After sandbox network is — see horizon-7 roadmap.md
- 2026-08-30 | horizon 7 | env-driven-baseurl-model-extension | During the anthropic phase follow-up (post-mark-done), the user requested baseUrl + model env-var support on the same pattern as the existing ANTHRO — see horizon-7 roadmap.md
- 2026-08-30 | horizon 7 | gemini-must-use-header-auth | The horizon-7 gemini phase rubric's URL-key-safety dimension asserts `entries[0].url` does NOT contain the literal apiKey. `src/interceptor.ts deriveUrl()` — see horizon-7 roadmap.md
- 2026-08-30 | horizon 8 | load-bearing-bypass-risk | Node 18+ global fetch is undici-backed and bypasses http.ClientRequest.prototype; the SDK's `fetch` option is the seam [src/interceptor.ts] -> horizon wraps S — see horizon-8 roadmap.md
- 2026-08-30 | horizon 8 | horizon-7-discovery-contradiction | horizon-7 claim 'global fetch routes through http.ClientRequest under Node 18+' is empirically wrong; fetch is undici [docs/roadmaps/llm-http-proxy/d — see horizon-8 roadmap.md
- 2026-08-30 | horizon 8 | interceptor-patch-surface | patch is http.ClientRequest.prototype at lines 225-228; covers node:http and node:https only, not undici [src/interceptor.ts] -> any SDK test shim must trans — see horizon-8 roadmap.md
- 2026-08-30 | horizon 8 | existing-integration-test-gate-shape | horizon-7 raw-HTTPS tests use if(hasKey) describe/else describe.skip with length>0 check + two-setImmediate flush + JSON.stringify no-key safety [ — see horizon-8 roadmap.md
- 2026-08-30 | horizon 8 | ai-sdk-npm-package-structure | ai + @ai-sdk/anthropic/openai/google are v5.x; each provider factory exposes `fetch: (input, init) => Promise<Response>` as documented interception seam [ — see horizon-8 roadmap.md
- 2026-08-30 | horizon 8 | tsconfig-typecheck-surface | tsconfig has types:['node','jest'] with @types/node@22 + @types/jest@29; SDK packages ship own types [tsconfig.json] -> no tsconfig changes or extra @types — see horizon-8 roadmap.md
- 2026-08-30 | horizon 8 | existing-install-state | Vercel AI SDK NOT installed anywhere; no entry for ai or @ai-sdk/* in package.json, node_modules, or lockfile [package.json] -> green-field install with no exis — see horizon-8 roadmap.md
- 2026-08-30 | horizon 8 | zero-lockfile-convention | package-level .gitignore contains package-lock.json; pnpm-lock.yaml is the committed lockfile [.gitignore] -> any new package-lock.json from npm install is gi — see horizon-8 roadmap.md
- 2026-08-30 | horizon 8 | gemini-sdk-key-leak-safety | horizon-7 gemini raw-HTTPS test uses x-goog-api-key header + asserts URL and JSON.stringify don't contain apiKey; deriveUrl passes req.path verbatim [src/ge — see horizon-8 roadmap.md
- 2026-08-30 | horizon 8 | default-parser-coverage | default parser extracts via usage.completion_tokens/output_tokens; Gemini's usageMetadata/camelCase not recognized, outputTokens falls back to chars/4 [src/pro — see horizon-8 roadmap.md
- 2026-08-30 | horizon 8 | test-file-discovery | testRegex matches *.integration.test.ts and *.sdk.integration.test.ts; no testPathIgnorePatterns [package.json] -> no jest config change needed for SDK tests; ms-s — see horizon-8 roadmap.md
- 2026-08-30 | horizon 8 | index-public-surface-invariance | src/index.ts exports fixed public surface; SDK packages not re-exported [src/index.ts] -> SDK tests must import directly from ai, @ai-sdk/*, never via — see horizon-8 roadmap.md
- 2026-08-30 | horizon 9 | undici-not-user-importable | Node 22.14.0 bundles undici 6.21.1 as globalThis.fetch's implementation but it is NOT reachable as a user-importable module: `require('undici')` from any pa — see horizon-9 roadmap.md
- 2026-08-30 | horizon 9 | undici-Dispatcher-types-shape | The Dispatcher class shape from undici-types@6.21.0: `dispatch(options: Dispatcher.DispatchOptions, handler: Dispatcher.DispatchHandlers): boolean` plus — see horizon-9 roadmap.md
- 2026-08-30 | horizon 9 | setGlobalDispatcher-round-trip | undici-types signature is `setGlobalDispatcher<D extends Dispatcher>(dispatcher: D): void` and `getGlobalDispatcher(): Dispatcher`; the type does NOT in — see horizon-9 roadmap.md
- 2026-08-30 | horizon 9 | dual-patch-double-emission-mitigation | globalThis.fetch and the SDK's fetch option both go through undici's global dispatcher in Node 22; if both surfaces fire for the same logical req — see horizon-9 roadmap.md
- 2026-08-30 | horizon 9 | horizon-7-undici-claim-was-wrong | horizon-7 discoveries entry claimed 'engines.node >=18; global fetch routes through http.ClientRequest.prototype' which is empirically wrong; fetch is — see horizon-9 roadmap.md
- 2026-08-30 | horizon 9 | undici-fetch-body-is-async-generator | globalThis.fetch(...) routes to undici's global dispatcher and undici hands `options.body` to dispatch() as an AsyncGenerator (typeof 'object', to — see horizon-9 roadmap.md
- 2026-08-30 | horizon 9 | callertrace-undici-internals | captureCallerTrace() runs inside WrappingDispatcher.dispatch() which is called synchronously from undici's internal dispatch path; the resulting callerTra — see horizon-9 roadmap.md
- 2026-08-30 | horizon 9 | asynciterable-body-needs-tee | undici's fetch wraps the request body in an AsyncIterable, so the natural body-capture pattern (consume the iterator via for-await) double-consumes the so — see horizon-9 roadmap.md
- 2026-08-30 | horizon 9 | jest-29-node-22-fetch-bridge-broken | Jest 29.7.0 + Node 22.14.0 + testEnvironment: 'node' exposes `globalThis.fetch` via Node's bootstrap lazy-loader that spawns a fresh BUNDLED undici — see horizon-9 roadmap.md
- 2026-08-30 | horizon 9 | global-fetch-capture-integration-test-landed | packages/llm-http-proxy/src/global-fetch-capture.integration.test.ts proves the dual-patch via `ud.fetch(url, {method:'POST', body: JSON.s — see horizon-9 roadmap.md
- 2026-08-30 | horizon 10 | repo-root-pnpm-lock-is-for-a-different-package | repo-root pnpm-lock.yaml has @nestjs/* importers and is NOT the llm-http-proxy lockfile; packages/llm-http-proxy/pnpm-lock.yaml is the actual lockfile of record (committed since v0, updated by pnpm add). pnpm 10 with `pnpm add -D` from inside the subpackage updated the per-package lockfile, leaving the repo-root one untouched — see horizon-10 roadmap.md
- 2026-08-30 | horizon 10 | sdk-version-latest-resolved | `pnpm add -D ai @ai-sdk/anthropic @ai-sdk/openai @ai-sdk/google` (no version pin) resolved to ai@7.0.85 + @ai-sdk/{anthropic,openai,google}@4.x.x, not the v5.x the discovery assumed. SDK install + type-resolution still pass; the FetchFunction shape for downstream phases may differ from the v5-era assumption — see horizon-10 roadmap.md
