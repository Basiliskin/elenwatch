# Binding decisions
- 2026-08-28 | horizon 1 | domain-shape-fit for llm-http-proxy is 'technical' (interception/parsing/redaction machinery, no business rules) — because the whole DDD/layering checklist for later horizons keys off this classification
- 2026-08-28 | horizon 1 | the extension-seams requirement is shipped as interface-only extension points, never built-in retries/streaming/TLS/WS/auth providers — because YAGNI gate 3 has no second implementation on the horizon
- 2026-08-28 | horizon 1 | latency benchmark, transformer pipeline, and OTEL exporter are full-objective success bars deferred to horizon 2, not dropped — because horizon-0 successCriteria explicitly do not certify them and the deferred entries carry the markers
- 2026-08-28 | horizon 2 | horizon 2 is a review-fixes pass on correctness findings #1-#7; the vision-track work (latency benchmark, transformer pipeline, OTEL exporter) slides to horizon 4+ — because the code review surfaced correctness bugs that must land before further feature work
- 2026-08-28 | horizon 2 | finding #9 (collapse the fake per-provider parser registry) is a deliberate breaking change to packages/llm-http-proxy/src/index.ts deferred to its own horizon — because it must update index.ts exports and the src/app.module.ts consumer in lockstep with a package rebuild, unlike the isolated correctness fixes

- 2026-08-28 | horizon 3 | #9 registry collapse is a clean break (drop resolveParser/parseCall from index.ts, VERSION bump to 0.2.0 in index.ts and package.json, sole consumer app.module.ts updated in lockstep) — because the package is unpublished 0.1.0 with one in-repo consumer, deprecated shims are YAGNI
- 2026-08-28 | horizon 3 | the transformer pipeline first slice (request strand + Content-Length accounting) is promoted from vision-deferred to an ADR-bound contract (docs/roadmaps/llm-http-proxy/transformer-slice-spec.md) — because the user explicitly promoted it into this horizon; the response strand stays deferred to horizon 4
- 2026-08-28 | horizon 4 | the transformer-slice ADR is re-pinned for the streaming boundary: SSE per-event responseTransform in scope, async/gzip/request-strand transforms stay §6 exclusions, buffered-path ordering (transform-before-caller / JSON.parse-post-transform / redaction-after) kept verbatim with a parallel per-event clause, capturePayloads=true retains only redacted masked text — because horizon 4 lands real SSE event-stream support
- 2026-08-28 | horizon 4 | latency-benchmark methodology + budget numbers are signed off per the decision recorded in horizon-04 roadmap (harness/hardware/warmup/iterations/measurement points incl. response-data path; p50/p99 anchors <1ms/<2%; vision's 'no work beyond body capture on the request path' bar is superseded — requestTransform now runs on the request path) — because a later horizon must execute the benchmark with no further decisions
- 2026-08-29 | horizon 5 | package identity is pinned: name llm-http-proxy, version 0.2.0, license MIT (matches package.json + index.ts VERSION; decision-time registry check `npm view llm-http-proxy` on 2026-08-29 returned HTTP 404 from https://registry.npmjs.org/llm-http-proxy — name free at decision time; streaming/responseTransform additions stay same-version additive on 0.2.0, a 0.3.0 bump is reserved for any future breaking change) — because the publish-verification bar cannot start while the name/version/license choice is open; missing package README.md + empty author field are recorded publish-hygiene debt for that bar
- 2026-08-29 | horizon 5 | OTEL posture: @opentelemetry/* packages are optional peer dependencies, never hard or direct dependencies, activated lazily through the existing Logger seam (src/logger.ts, src/index.ts doc comments) only when those peer deps are installed — because the OTEL span-exporter demo must not force a hard runtime dependency on every package consumer; non-goals this horizon: no @opentelemetry/* dependency is installed and no activation code ships; the OTEL span-exporter demo bar stays deferred and is merely unblocked for a later horizon
- 2026-08-29 | horizon 6 | package author is 'Dimitry Katz <dimitry.kazt@gmail.com>' (git-config identity, user-confirmed at the horizon-6 alignment preview) — because the publish bar needs a non-empty author and the identity must not reopen the pinned package-identity decision (llm-http-proxy 0.2.0, MIT)
- 2026-08-29 | horizon 6 | otelSpanLogger is re-exported from the package root (src/index.ts) as the consumer-facing OTEL demo surface, and the package-lock.json created by installing optional-peer devDeps stays gitignored per the repo's zero-lockfile convention — because the exports map exposes only the root entry and the repo deliberately keeps zero lockfiles; both constrain the future real-publish bar
- 2026-08-30 | horizon 7 | integration tests gate on API-key env var presence (skip when unset), no separate *_LIVE flag — the API key IS the opt-in; a separate flag would fail-not-skip when set without the key
- 2026-08-30 | horizon 7 | the fetch baseline integration test runs BY DEFAULT (no opt-in gate) and uses a localhost-only HTTPS round-trip via openssl-generated self-signed cert + node:https createServer on 127.0.0.1 — because the baseline exercises only local infrastructure (no credentials, no external host, no DNS) and running it by default proves plain-HTTPS interception on every CI run; the API-key-gated opt-in pattern is reserved for the three LLM-provider tests that MUST reach external APIs
